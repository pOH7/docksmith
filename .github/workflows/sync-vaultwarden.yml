name: Sync Vaultwarden

on:
  schedule:
    - cron: '0 18 * * *'  # Daily at 2 AM UTC+8 (18:00 UTC)
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-multi-template.yml
    with:
      config: |
        {
          "version_key": "dani-garcia/vaultwarden",
          "source_repo": "dani-garcia/vaultwarden",
          "sync_type": "release",
          "components": [
            {
              "type": "image",
              "images": ["vaultwarden/server"]
            }
          ]
        }
    secrets: inherit
