name: Sync Homer

on:
  schedule:
    - cron: '0 18 * * *'  # Daily at 2 AM UTC+8 (18:00 UTC)
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-multi-template.yml
    with:
      config: |
        {
          "version_key": "bastienwirtz/homer",
          "source_repo": "bastienwirtz/homer",
          "sync_type": "release",
          "components": [
            {
              "type": "image",
              "images": ["b4bz/homer"]
            }
          ]
        }
    secrets: inherit
