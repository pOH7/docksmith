name: Sync Resilio Sync

on:
  schedule:
    - cron: '0 18 * * *'  # Daily at 2 AM UTC+8 (18:00 UTC)
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-multi-template.yml
    with:
      config: |
        {
          "version_key": "linuxserver/docker-resilio-sync",
          "source_repo": "linuxserver/docker-resilio-sync",
          "sync_type": "release",
          "components": [
            {
              "type": "image",
              "images": ["lscr.io/linuxserver/resilio-sync"]
            }
          ]
        }
    secrets: inherit
