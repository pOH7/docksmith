name: Sync v2rayA

on:
  schedule:
    - cron: '0 18 * * *'  # Daily at 2 AM UTC+8 (18:00 UTC)
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-multi-template.yml
    with:
      config: |
        {
          "version_key": "v2rayA/v2rayA",
          "source_repo": "v2rayA/v2rayA",
          "sync_type": "release",
          "components": [
            {
              "type": "image",
              "images": ["mzz2017/v2raya"]
            }
          ]
        }
    secrets: inherit
