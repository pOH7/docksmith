name: Sync v2rayA

on:
  schedule:
    - cron: '0 18 * * *'  # Daily at 2 AM UTC+8 (18:00 UTC)
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-template.yml
    with:
      repo: 'v2rayA/v2rayA'
      sync_type: 'release'
      source_images: |
        - mzz2017/v2raya
    secrets: inherit
